# 本地验证结果

2026-09-16，运行 `pdv-db190d91e7`：**29 组检查全部通过**。

本文保留该次本地验收范围；随后完成的 Go 云端验证单独记录在 [CLOUD-RESULTS.md](CLOUD-RESULTS.md)。

开始：北京时间 14:38:05；完成：14:40:57。本次复用已有构建缓存，该耗时不代表首次下载、构建或云端性能。

## 实际验证范围

应用镜像为 **Linux/amd64**；Go、Python、Node、Rust 分别构建 v1/v2，共 8 个不同版本镜像，最终运行用户均为非 root。两个专用 PostgreSQL 容器使用本机已有 PostgreSQL 16 镜像，数据库镜像架构为 arm64；通过标准 PostgreSQL 协议连接。

| 检查组 | Go | Python | Node | Rust |
| --- | --- | --- | --- | --- |
| 缺失验证令牌时拒绝启动 | 通过 | 通过 | 通过 | 通过 |
| 鉴权、读写、重复请求、Unicode、参数化 SQL、输入边界 | 通过 | 通过 | 通过 | 通过 |
| 修改源码、重建及替换镜像、变更端口，数据保留 | 通过 | 通过 | 通过 | 通过 |
| 读取四种语言各自写入的数据 | 通过 | 通过 | 通过 | 通过 |
| 数据库中断返回 503，恢复后无需重启应用 | 通过 | 通过 | 通过 | 通过 |
| 默认 TLS 不被 URI 的 `sslmode=disable` / `ssl=0` 降级 | 通过 | 通过 | 通过 | 通过 |
| 信任 CA 成功，非受信证书和错误主机名被拒绝 | 通过 | 通过 | 通过 | 通过 |

额外一组迁移检查通过：停止本次应用写入 → `pg_dump` → 第二个专用 PostgreSQL 中恢复 → 四条业务记录完全匹配 → 验证自增序列及目标独立新增数据。源库在检查完成前保留，最终与其他临时资源一起清理。该检查没有模拟目标库产生新业务数据后的反向迁移。

检查过程中还复核了 Node pg 驱动的 `ssl=0` 参数覆盖行为；样例统一清理 URI 内 TLS 参数，并以独立的 `DATABASE_SSLMODE` 策略控制验证。

## 证据与清理

- [原始 JSON 报告](results/local-container-report.json)：逐组结果、镜像 ID、时间和迁移记录数量。
- [验证源码摘要](results/source-sha256.json)：对应此次通过检查的脚本、样例和依赖锁文件。
- 详细构建和脱敏运行日志留在本机 `validation/artifacts/pdv-db190d91e7/`，该目录不纳入版本控制。
- 临时容器、数据库卷和网络已由脚本删除；另行核对本次运行标签下无容器或网络残留。临时凭证与 TLS 私钥目录已删除。
- 本地镜像和构建缓存保留，便于复现；未执行全局 Docker 清理。

## 尚未验证

- Railway/Neon 或其他真实云账号的正式 Free 资格、API 授权、网络可达性、云端构建及部署。
- 实际云费用、额度耗尽和恢复行为、云端冷启动。
- 真实业务库、不同 PostgreSQL 主版本或供应商特有扩展的迁移。
- 应用镜像的 arm64 运行路径；本次应用只验收 Linux/amd64。
- 正式本地管理服务、Web UI、Agent 接口与云平台适配器，均尚未实现。

本轮没有连接已有数据库、创建云资源、使用公司代码或公司云账号。
